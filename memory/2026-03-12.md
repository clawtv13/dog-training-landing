# 2026-03-12

- Cron pass discovered that the earlier product template section disables did not persist.
- Legacy sections that were previously disabled had somehow re-enabled, meaning the contamination risk returned to the live product page.
- Re-applied disable logic to all non-main sections in `templates/product.json`.
- Verified that only the `main` section is now enabled on the live product template.
- This issue was a meaningful blocker because it meant the store could have been showing old posture-product content again.
