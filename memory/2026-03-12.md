# 2026-03-12

- Cron pass discovered that the earlier product template section disables did not persist.
- Legacy sections that were previously disabled had somehow re-enabled, meaning the contamination risk returned to the live product page.
- Re-applied disable logic to all non-main sections in `templates/product.json`.
- Verified that only the `main` section is now enabled on the live product template.
- This issue was a meaningful blocker because it meant the store could have been showing old posture-product content again.

## Session 00:41 UTC - Re-enable blocker persists

- Re-checked product template state and found legacy sections are **still enabled** despite multiple prior disable attempts.
- This indicates an external process (theme editor, another script, or concurrent user edits) is overriding the programmatic disables.
- **Real blocker:** cannot reliably keep legacy product sections disabled via API-only asset writes.
- Autonomous passes should pause on product template cleanup until the root cause is identified or resolved manually.
- User may need to disable those sections directly in the Shopify theme editor or identify what is re-enabling them.
